{% extends 'base.html.twig' %}

{% block title %}Crear usuario{% endblock %}
{% block stylesheets %}{{ parent() }}{% endblock %}
{% block body %}
<div class="container">
	{# <main class=""> #}
		<div class="card mt-4">
		    <div class="card-body">
			  	<div class="">
			    	<h4>Usuario | Editar Usuario</h4>
				</div>
			</div>
		</div>
	    <div class="card mt-4">
		  <div class="card-body">

			  <form action="{{ path('user.update', {id: user.Id} ) }}" method="POST">
			  	
				<div class="form-row">
					<div class="form-group col-md-6">
						<label for="name">Nombre</label>
						<input type="text" name="name" class="form-control" id="name" value="{{ user.Name }}" required>
					</div>
					<div class="form-group col-md-6">
						<label for="last_name">Apellido</label>
						<input type="text" name="last_name" class="form-control" id="last_name" value="{{ user.LastName }}" required>
					</div>
				</div>
				<div class="form-row">
					<div class="form-group col-md-6">
						<label for="email">Correo eléctronico</label>
						<input type="email" name="email" class="form-control" id="email" value="{{ user.Email }}" required>
					</div>
					<div class="form-group col-md-6">
						<label for="document">Cedula</label>
						<input type="text" name="document" class="form-control" id="document" value="{{ user.Document }}" required>
					</div>
				</div>
				<div class="col-12 mt-3 text-center">
                    	<a href="{{path('user.index')}}" class="btn btn-sm btn-secondary">
                        Volver
                    	</a>
                    	<button type="submit" class="btn btn-sm btn-success">
                    	Aceptar
                    	</button>
            	  </div>
			  </form>
		  </div>
		</div>
    {# </main> #}
</div>
{% endblock %}

